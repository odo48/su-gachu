# Dashboard — overrides

Keep Master colors.

- KPI / metric blocks as `Card`, not ad-hoc bordered divs.
- Charts: existing Recharts; tooltip on hover; don’t add ornamental chrome.
- Primary page action (generate plan) = `Button` with loading/disabled.
- Filters and provider select stay compact; don’t turn this into an enterprise BI wall of widgets.
