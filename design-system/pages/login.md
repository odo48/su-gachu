# Login — overrides

Keep Master colors (no rose/purple palettes).

- Centered `Card` (`max-w-sm`), heading in `font-heading`.
- Social actions first: Google (`Button variant="outline"`), Apple (black, white text).
- Then `Separator` with “sau”, then email `Label` + `Input` + primary `Button`.
- Errors as `role="alert"` text under the form; OAuth failures also via `?error=oauth`.
- Min height 44px on every action.
