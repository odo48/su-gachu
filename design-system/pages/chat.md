# Chat — overrides

Keep Master colors (not lavender).

- Soft card thread on `bg-background`; assistant/user bubbles with clear contrast.
- Sidebar list uses `Button variant="ghost"` for conversations.
- Composer: `Input` + send `Button`; disable send while streaming.
- No neon accents; Lucide icons only.
